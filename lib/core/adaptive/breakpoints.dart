class AppBreakpoints {
  AppBreakpoints._();

  /// Minimum width at which the shell switches from bottom navigation
  /// (phone) to a navigation rail (tablet/desktop).
  static const double desktop = 700;
}
